import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    buttonContainer: {
        width: 270,
    },
    button: {
        alignSelf: 'center',
        borderRadius: 10,
        paddingVertical: 15,
        width: '90%'
    },
    buttonText: {
        textAlign: 'center',
    }
})

export default styles